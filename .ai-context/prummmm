Lee los archivos en `.ai-context/` (implementation_plan.md, task.md, walkthrough.md) y la
sección "Backend (Google Cloud Run)" del README.md. Ahí está todo el contexto del proyecto.

SITUACIÓN ACTUAL:
- Phase 0, 1A, 1B, 1C, 1D: COMPLETADAS ✅
- Backend desplegado y funcionando en Cloud Run (revision 00046-hfx, API 200 OK)
- Frontend en Cloudflare Pages (auth PKCE funcionando)
- CI/CD: push a main → Cloud Build auto-deploya (Build→Push→Deploy)

PROBLEMA DETECTADO: se envió un mensaje al chat de prueba de WhatsApp y NO hubo respuesta
del bot ni registro en Sentry ni Discord. Esto indica que el webhook o el pipeline
LLM no está funcionando correctamente en producción.

SIGUIENTE FASE: Phase 2 (Sentry Exhaustivo) + Phase 3 (E2E Validation)
Se deben ejecutar juntas: primero asegurar que Sentry capture TODO, luego testear cada
componente y herramienta del LLM una por una.

REGLAS NO NEGOCIABLES:
1. DOCS-FIRST: Antes de implementar CUALQUIER cambio, leer la documentación oficial
   actualizada del servicio involucrado. Si no está en los docs, no se asume.
2. CERO REGRESIONES: Antes de modificar código para corregir un error, verificar que
   las funcionalidades existentes siguen funcionando. Si se rompe algo al arreglar
   otro, se revierte INMEDIATAMENTE. Esto es crítico — la semana pasada se perdió
   entera en loops de regresión.
3. DOCUMENTAR TODO: Apenas una solución funcione, documentar EN ESE MOMENTO por qué
   funciona (con referencia a los docs oficiales) tanto en los artefactos como en el
   README. No dejar para después.
4. SENTRY PRIMERO: Ningún fix se considera "completo" si el error/éxito no queda
   capturado en Sentry con traceback completo.

INFRAESTRUCTURA:
- GCP Project: saas-javiera
- Cloud Run: ia-backend-prod (europe-west1)
- SA: ia-calendar-bot@saas-javiera.iam.gserviceaccount.com
- Supabase prod: nemrjlimrnrusodivtoa
- Frontend: dash.tuasistentevirtual.cl (Cloudflare Pages)
- Sentry DSN (backend): hardcoded en main.py (TODO: mover a env var)

Empieza leyendo los archivos de contexto mencionados arriba, luego diagnostica
por qué el webhook de WhatsApp no genera respuesta ni logs.
